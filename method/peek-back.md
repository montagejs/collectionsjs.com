---

name: peekBack()

see:
-   poke-back
-   peek
-   pop

samples:
- |
    var array = [1, 2, 3];
    array.peekBack();

---

Returns the value at the end of a collection, the value that would be
returned by `pop()`.

--- |

