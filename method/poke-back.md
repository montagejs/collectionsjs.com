---

name: pokeBack(value)

see:
-   peek-back
-   poke
-   pop
-   push

samples:
- |
    var array = [1, 2, 3];
    array.pokeBack(4);
    array

---

Replaces the value at the end of a collection, the value that would be
returned by `pop()`.

--- |

